class Cat {
    // Field (variable)
    int number;

    // Constructor
    Cat(int number) {
        this.number = number;
    }

    // Method to print the value
    void printNumber() {
        System.out.println("Number: " + number);
    }
}